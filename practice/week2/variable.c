#include <stdio.h>

int main()
{
	int x;
	int y;
	int z;

	// int 타입의 x,y,z라는 이름을 가진 변수 생성

	x = 1;
	y = 2;

	// x,y의 변수에 1,2라는 값 대입
	
	z = x + y;

	// 연산기호를 통해 변수 x와 y를 계산한 값을 z에 대입

	printf("%d", z);

	return 0;
}